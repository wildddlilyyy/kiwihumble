/.phpunit.cache
/node_modules
# Keep the production Vite bundle in version control so Git-based deployments
# serve the same compiled styles as the local production build.
/public/hot
/storage/*.key
/vendor
.env
.env.backup
.env.production
Homestead.json
Homestead.yaml
auth.json
npm-debug.log
yarn-error.log
/.fleet
/.idea
/.vscode
